services:
  - type: web
    name: bg-remover-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: PROXY_SECRET
        generateValue: true   # Render generates a random secret; copy it into RapidAPI proxy header
